# You can use this file to create a conda environment for this pipeline:
#   conda env create -f environment.yml
name: nanopore_mapping
prefix: /usr/bin
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - minimap2
  - samtools
  - survivor
  - svim
  - pysam
  - shasta
  - bedtools
  - pandas
  - cyvcf2
  - ensembl-vep
  - scikit-allel
  - bcftools
